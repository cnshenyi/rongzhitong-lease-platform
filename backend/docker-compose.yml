version: '3.8'

services:
  postgres:
    image: postgres:15
    container_name: rongzhitong-postgres
    environment:
      POSTGRES_DB: rongzhitong
      POSTGRES_USER: rongzhitong
      POSTGRES_PASSWORD: rongzhitong123
    ports:
      - "5433:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data
    restart: unless-stopped

  redis:
    image: redis:7-alpine
    container_name: rongzhitong-redis
    ports:
      - "6379:6379"
    volumes:
      - redis_data:/data
    restart: unless-stopped

volumes:
  postgres_data:
  redis_data:
